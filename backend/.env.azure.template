# Azure Web App Configuration Template
# Copy these environment variables to Azure Portal → Web App → Configuration → Application Settings

# Database Configuration
MONGODB_CONNECTION_STRING=<your_mongodb_atlas_connection_string>
DATABASE_NAME=<your_database_name>

# Azure Storage Configuration  
AZURE_CONTAINER_URL=<your_azure_blob_container_url>
AZURE_CONTAINER_SAS=<your_azure_sas_token>

# Python Configuration
PYTHONPATH=/home/site/wwwroot
SCM_DO_BUILD_DURING_DEPLOYMENT=true

# Flask Configuration (optional)
FLASK_ENV=production
FLASK_DEBUG=false

# Port Configuration (Azure sets this automatically, but included for reference)
# PORT=80

# Notes:
# 1. Replace <placeholder> values with your actual credentials
# 2. Do not commit this file with actual secrets
# 3. Set these in Azure Portal → Configuration → Application Settings
# 4. These same values should be in your Render.com environment for UAT